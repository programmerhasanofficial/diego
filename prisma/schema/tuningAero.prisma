model TuningAero {
  id                String @id @default(uuid()) @db.Uuid
  advancedCarDataId String @db.Uuid @unique
  advancedCarData   AdvancedCarData @relation(fields: [advancedCarDataId], references: [id])

  ecuType    String?
  aeroParts  String?
}